import { buildDefaultEntries } from './entryTemplates';

/** Primary starter profile (Close month). Prefer entry templates / sets for new code. */
export const defaultEntries = buildDefaultEntries();
